services:
  - type: web
    name: chat-backend
    env: node
    buildCommand: npm install && npm run build && ls -la dist/
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production

